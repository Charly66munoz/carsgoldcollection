<form method="post" action="{{ path('delete_user', {'id': user.id})}}" onsubmit="return confirm('Esta seguro de que desea eliminar {{
user.nombre}}?');">
    <input type="hidden" name="_token" value="{{csrf_token('delete' ~ user.id) }}">
    <button class="">Borrar</button>
</form>

